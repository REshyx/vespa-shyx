# vtkSHYXVmtkOpeningCenterlines

从开口种子点算 VMTK 中心线。需 `VESPA_USE_VMTK`。端口 0 可选中心线，端口 1 Opening seed points（首次显示默认 **Point Label**，不是 Point Gaussian）。面板 `shyx_openings_table`。

参数见同目录 `SHYXVmtkOpeningCenterlines.xml`。
