# vtkSHYXVmtkPolyDataCenterlines

VMTK 中心线。需 `VESPA_USE_VMTK`，且 VMTK 与 ParaView 的 VTK 匹配。三个输入：表面 + `SourceSeeds` + `TargetSeeds`；一个输出。不在 Vascular 工具条。

参数见 `ParaViewPlugin/SHYXVmtkPolyDataCenterlines.xml`。
