# vtkSHYXFlipSelectedCellsWindingFilter

菜单 **SHYX Flip Selected Cells Winding**。翻转选中单元的顶点绕向。端口 0 `vtkPolyData`，端口 1 可选 `vtkSelection`，或 `SelectionCellArrayName`。**若选区与 mask 都为空，则翻转所有单元。**

参数见 `ParaViewPlugin/SHYXFlipSelectedCellsWinding.xml`。
