# vtkSHYXPartitionedCollectionWslSimulation

把 Partitioned Dataset Collection 经 **VTK IOSS** 导出到 WSL、跑算、再读回。不在 Vascular 工具条。XML 默认 Work Directory 形如 `\\wsl.localhost\Ubuntu-24.04\home\shyx\Test`（开发机约定，按本机改）。

参数见同目录 `SHYXPartitionedCollectionWslSimulation.xml`。
