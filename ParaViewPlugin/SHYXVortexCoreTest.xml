<ServerManagerConfiguration>
  <ProxyGroup name="filters">
    <SourceProxy name="SHYXVortexCoreTest"
                 class="vtkVortexCoreTest"
                 label="SHYX Vortex Core (Test)">
      <Documentation long_help="Same algorithm as VTK Vortex Cores (parallel vectors); provided as a VESPA plugin copy for experiments."
                     short_help="Vortex core lines (vtkVortexCoreTest plugin).">
        Compute vortex core lines using the parallel vectors method (identical logic to vtkVortexCore).
        Optional higher-order mode uses velocity and jerk; default uses velocity and acceleration.
        Q, delta, lambda_2, and lambda_ci criteria prefilter and are stored on output polylines.
      </Documentation>

      <InputProperty command="SetInputConnection"
                     name="Input">
        <ProxyGroupDomain name="groups">
          <Group name="sources"/>
          <Group name="filters"/>
        </ProxyGroupDomain>
        <DataTypeDomain name="input_type">
          <DataType value="vtkDataSet"/>
        </DataTypeDomain>
        <InputArrayDomain name="input_array1"
                          number_of_components="3"
                          optional="0"/>
        <Documentation>
          The input.
        </Documentation>
      </InputProperty>

      <StringVectorProperty name="SelectInputArray"
                            command="SetInputArrayToProcess"
                            number_of_elements="5"
                            element_types="0 0 0 0 2"
                            label="Vector Field">
        <ArrayListDomain name="array_list" attribute_type="Vectors"
                         input_domain_name="input_array1">
          <RequiredProperties>
            <Property name="Input" function="Input"/>
          </RequiredProperties>
        </ArrayListDomain>
        <Documentation>
          Vector array from which to compute vortex cores.
        </Documentation>
      </StringVectorProperty>

      <IntVectorProperty command="SetHigherOrderMethod"
                         name="UseHigherOrderMethod"
                         default_values="0"
                         number_of_elements="1">
        <BooleanDomain name="bool"/>
        <Documentation>
          Use jerk instead of acceleration as the second vector field in the parallel vector operation.
        </Documentation>
      </IntVectorProperty>

      <IntVectorProperty command="SetFasterApproximation"
                         name="FasterApproximation"
                         default_values="0"
                         number_of_elements="1">
        <BooleanDomain name="bool"/>
        <Documentation>
          Faster approximate gradients (fewer derivative calculations).
        </Documentation>
      </IntVectorProperty>

      <Hints>
        <ShowInMenu category="SHYX" icon=":/VESPA/SHYX_Vortex_Core_Test.png"/>
      </Hints>
    </SourceProxy>
  </ProxyGroup>
</ServerManagerConfiguration>
